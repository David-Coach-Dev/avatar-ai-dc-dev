import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'avatar-ai-dc-dev',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
